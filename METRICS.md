# forge の性能指標

**指標の計算は無料**。`archive.jsonl` を読むだけで、LLMもネットワークも使わない。
だから指標を1つに絞らず、全部計算して並べる。指標を選び直しても再走行は要らない。

計算は [forge/metrics.py](forge/metrics.py)、集計と比較は
[tools/analyze_runs.py](tools/analyze_runs.py)。

```bash
# 1群を全指標で見る
python3 tools/analyze_runs.py 'runs/bench/bench_obp/20260726/seed*'

# 2群を比較する（検出力つき）
python3 tools/analyze_runs.py --control 'runs/bench/*_ctl/*/seed*' \
                              --treatment 'runs/bench/*_trt/*/seed*'

# API失敗のばらつきを消したいときは、生存候補数で揃える
python3 tools/analyze_runs.py --candidate-cap 200 ...
```

## なぜ1指標では駄目なのか

既定だった `best_score`（1走行の最大値）は、最大値統計なので本質的に分散が大きい。
2026-07-25〜27 の24走行の実測値。

| 指標 | 標準偏差 | MDE(n=3) | MDE(n=10) |
|---|---|---|---|
| best_score | 19.1 | **45.2** | 24.8 |
| auc_by_generation | 11.8 | 27.9 | 15.3 |
| **auc_by_candidate** | **10.5** | **25.0** | **13.7** |

**MDE**（最小検出可能効果）= そのシード数で拾える最小の群間差。

bench_obp では、人手の best-fit（-2091.8）から FunSearch 公開値相当（-2025.1）まで、
**伸びしろ全体が 66.7 点しかない**。`best_score` の MDE 45.2 はその 68% にあたる。
つまり「forge を世界記録級にする改良」ですら 3 シードでは有意にならない。

実例。`max_per_score` の A/B を19指標で見直すと、有意だったのは **`hit_rate` の1つだけ**だった。

| 指標 | 対照 | 実験 | 差 | MDE | 判定 |
|---|---|---|---|---|---|
| best_score | -2058.07 | -2088.80 | -30.73 | 56.8 | 判定不能 |
| **hit_rate** | 0.116 | 0.014 | -0.102 | 0.056 | **有意** |

「3シード全敗」は勝敗の数え方であって統計的判定ではなかった。結論（max_per_score は有害）は
正しかったが、根拠にしていた指標では判定できていなかった。

## 指標一覧

### 到達点

| 指標 | 意味 |
|---|---|
| `best_score` | 最終到達スコア。分散が大きいので単独では使わない |
| `best_excess_over_lb_pct` | 理論下界からの超過率(%)。**問題を跨いで比較でき、論文値と直接並べられる** |
| `top5_mean` / `top10_mean` | 上位の平均。単発の外れ値に引かれにくい |
| `beats_baseline` / `gain_over_baseline` | 人手のヒューリスティックを超えたか、何点超えたか |

### 軌跡

| 指標 | 意味 |
|---|---|
| **`auc_by_candidate`** | best-so-far 曲線の平均（生存候補数を横軸に）。**分散最小。既定の主指標** |
| `auc_by_generation` | 同じく世代を横軸に。API失敗のばらつきを拾うので候補軸より劣る |
| `candidates_to_beat_baseline` | 何候補目で基準を超えたか。超えなければ `None`（0で埋めない） |
| `final_best_gen` | 最終解が出た世代。収束の早さ |
| `largest_single_jump` | 1回の更新での最大の伸び。飛躍が起きたか |
| `bands` | 世代帯ごとの更新ハザードと獲得点。**世代を伸ばすかの判断はこれで行う** |

### 探索の健全性

| 指標 | 意味 |
|---|---|
| `distinct_scores` / `distinct_scores_ratio` | スコアの種類。潰れていないか |
| `catastrophic_rate` | 壊滅的な候補の割合。親プールの汚染を検知する |
| `hit_rate` | 基準超えの割合。**best_score より分散が小さいことがある** |
| `score_p50` / `score_p90` | 生存候補の分布 |
| `islands_used` | 実際に使われた島の数 |

### 効率

| 指標 | 意味 |
|---|---|
| `alive_per_call` | 1呼び出しあたり何件の生存候補が得られたか |
| `gain_per_call` | 1呼び出しあたりの改善点数。**推論基盤の投資判断に直結** |
| `cheap_failure_rate` | 応答が返らなかった割合 |
| `wall_secs` | 実時間 |

## 走行条件を揃える

走行間で条件が揃っていないと、指標の差が実力差なのか条件差なのか分からない。

- `--gen-cap N` 世代で揃える
- `--candidate-cap N` **生存候補数で揃える**（推奨）

API失敗率は実測で26〜67%と大きく振れる。世代で揃えると「たまたま失敗が少なかった走行」が
有利になる。候補数で揃えるとこの雑音が消える（標準偏差 11.8 → 10.5）。

## 判定の規則

1. **走行前に主指標と判定条件を決める。** 走ってから指標を選ぶと、都合の良い1つを拾える。
2. **差が MDE 以下なら「効果が無い」ではなく「測れていない」と書く。** この2つを混同しない。
3. **MDE が伸びしろ全体を超えているなら、その設計では何を変えても判定できない。**
   シードを増やすか、分散の小さい指標に変えるのが先で、改良を足すのは後。
4. **途中経過で暫定解釈を述べない。** 先に完走したシードほど順調だったシードである確率が高い
   （2026-07-27: 2シードの途中経過から「世代延長が効く」と2回報告し、完走後に覆った）。

## 参照点（bench_obp）

| | スコア | L1下界超過 |
|---|---|---|
| 理論下界 | — | 0% |
| FunSearch 公開値（未再現） | ≒ -2025.1 | 0.68% |
| **forge 最良（2026-07-25）** | **-2033.0** | **1.07%** |
| best-fit（人手の定番・シード） | -2091.8 | 4.00% |
| first-fit | -2099.4 | 4.38% |
